# 秋招投递助手：第一版需求

确认日期：2026-09-10

## 产品范围

面向本人使用的秋招投递记录工具。仅开发响应式网页，适配电脑和手机浏览器，两端访问同一份服务端数据。部署到已有云服务器，源代码使用 GitHub 管理。

不开发 Android App，不开放其他用户注册，投递信息全部手动录入。GitHub 用于管理源代码，不作为投递数据的存储位置。

## 已确认功能

### 投递记录

- 每条记录对应一次“公司 + 岗位”投递。同一公司的其他岗位建立新的记录。
- 信息包括公司名称、岗位名称、岗位链接、投递时间、当前进度和备注。
- 支持新增、查看和编辑记录。
- 支持搜索和筛选。

### 流程跟踪

固定阶段：已收藏、已投递、笔试、一面、二面、HR面、Offer、拒绝。

- Offer 和拒绝为不同结果，不要求依次经过。
- 允许跳过阶段和纠正状态。
- 保留每次状态变化的时间，详情页可查看流程历史。

### 日程与提醒

- 一条投递可关联多项日程，包括测评截止、笔试和面试。
- 每项日程可记录时间、链接及备注。
- 首页展示即将到期和已逾期事项。
- 仅做站内提醒。关闭网页后不发送通知，不接入邮件、短信、浏览器推送等渠道。

### 首页与统计

- 首页顶部显示简要统计，下方默认显示投递列表。
- 可切换为按阶段分列的看板。
- 支持按公司、时间和当前阶段搜索或筛选。
- 展示投递总数、各阶段数量、回复率和 Offer 数量。

### 访问与数据

- 个人密码登录，不开放注册。
- 电脑和手机浏览器共用服务端数据。
- 第一版不包含离线编辑和离线同步。

## 页面结构

1. 登录页：个人访问验证。
2. 首页：简要统计、即将到期和已逾期事项、搜索筛选、列表与看板切换。
3. 投递详情：基本信息、当前进度、状态历史、关联日程。
4. 新建和编辑表单：投递信息、日程信息；可采用弹窗或独立页面，按实现便利性决定。

## 开发默认规则

以下是为避免继续扩展需求而采用的实现默认值，可在开发或试用后调整，不属于用户逐项指定的细节。

- 公司、岗位和当前进度必填，其他字段允许稍后补充；已收藏记录可以没有投递时间。
- 时间按北京时间展示，输入界面明确标注时区。
- 即将到期默认指未来 7 天内未完成的日程；超过时间且未完成的日程视为逾期。
- 日程可标记完成或取消，避免持续显示为逾期。
- 可纠正流程历史中的录入错误，明确区分事件发生时间与系统记录时间。
- 允许删除误录的投递或日程，删除前确认。
- 投递总数按填写投递时间的记录统计，已收藏但未投递的记录不计入。
- 各阶段数量按当前状态统计，Offer 数量按当前状态为 Offer 的记录统计。
- 手动记录没有独立的“收到回复”字段，第一版将“已进入笔试或面试阶段，或当前为 Offer / 拒绝”的已投递记录占比作为流程反馈率，界面注明该口径，不将其表示为邮件实际回复率。历史上进入过相关阶段的记录按记录去重。
- 首次加载页面和保存操作后刷新数据，另提供手动刷新；第一版不要求跨浏览器即时推送。

## 简化架构

- 一个 Web 应用：提供响应式页面、个人登录和数据接口。
- 一个数据库：保存投递记录、状态历史和日程。
- 两类浏览器通过同一服务访问数据库，不直接连接数据库。
- 部署在用户已有云服务器。具体语言、框架、数据库和部署方式尚未选定，应结合服务器环境确定。
- 登录凭据、数据库文件和个人投递数据不提交至 GitHub。

核心数据关系：一条投递记录关联多条状态历史和多条日程。

## 第一版验收场景

1. 在电脑端新增投递，手机浏览器刷新后可看到同一条记录。
2. 同一公司新增两个不同岗位，分别维护进度，互不影响。
3. 修改状态后，列表、看板和统计保持一致，详情页可查看状态历史。
4. 创建测评截止或面试日程，首页按时间展示即将到期或逾期事项；完成或取消后不再提醒。
5. 公司、时间及阶段筛选结果正确；手机端可以正常新增、编辑和查看记录。
6. 未登录时不能访问投递页面或数据接口。
7. 重启服务后数据仍保留。

## 开发前待核实的部署信息

用户已确认有云服务器，但尚未提供操作系统、配置、现有服务、域名和 HTTPS 情况。这些信息在部署时核实，不继续扩展产品功能。

## 本阶段交付状态

需求访谈完成，本文作为第一版开发依据。第一版采用 Node.js 24、Express 5 和 SQLite 实现，运行、验证与部署方法见 README.md。实际云服务器部署需结合服务器环境完成。
